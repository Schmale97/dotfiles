- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.zshrc:

- create:
    - ~/Morrisons
    - ~/Repositories

- shell:
    - 
      command: ./gitconfig-morrisons/create.sh
      stdin: true
      stdout: true
      stderr: true
      description: Creating Default .gitconfig-morrisons

#   - [git submodule update --init --recursive, Installing submodules]